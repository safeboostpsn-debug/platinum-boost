name: Обновление платин

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Обновить index.html из data.json
        run: python generator.py

      - name: Запушить
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          git diff --staged --quiet || (git commit -m "Автообновление платин" && git push)
          
      - name: Скачать обложки из PS Store
        run: python download_covers.py

      - name: Обновить index.html
        run: python generator.py
